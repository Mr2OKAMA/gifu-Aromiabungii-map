name: Update Map

on:
  issues:
    types:
      - opened

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Show Issue
        run: |
          echo "${{ github.event.issue.body }}"
